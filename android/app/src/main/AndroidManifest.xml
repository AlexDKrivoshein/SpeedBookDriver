<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"> <!-- + добавили tools -->

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
            android:label="SpeedBook Taxi Driver"
            android:name=".MyApplication"
            android:enableOnBackInvokedCallback="true"
            android:icon="@mipmap/ic_launcher">

        <activity
                android:name=".MainActivity"
                android:exported="true"
                android:launchMode="singleTop"
                android:taskAffinity=""
                android:theme="@style/LaunchTheme"
                android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
                android:hardwareAccelerated="true"
                android:windowSoftInputMode="adjustResize">
            <meta-data
                    android:name="io.flutter.embedding.android.NormalTheme"
                    android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Flutter -->
        <meta-data android:name="flutterEmbedding" android:value="2" />
        <meta-data android:name="com.google.android.geo.API_KEY" android:value="${GOOGLE_GEO_API_KEY}"/>
        <meta-data android:name="GOOGLE_MAPS_API_KEY" android:value="${GOOGLE_MAPS_API_KEY}"/>
        <meta-data android:name="API_URL" android:value="${API_URL}"/>
        <meta-data android:name="com.google.firebase.messaging.default_notification_channel_id" android:value="default_channel" />

        <!-- === ОТКЛЮЧАЕМ Branch auto-init НА ВРЕМЯ ТЕСТОВ === -->
        <provider
                android:name="io.branch.referral.BranchInitProvider"
                android:authorities="${applicationId}.branchinitprovider"
                tools:node="remove" />
        <receiver
                android:name="io.branch.referral.InstallListener"
                tools:node="remove" />

        <!-- Удалите эту строку из main-манифеста (перенесите в debug/AndroidManifest.xml):
        <meta-data android:name="io.branch.sdk.TestMode" android:value="true"/>
        -->
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
